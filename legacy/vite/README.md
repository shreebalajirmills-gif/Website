Shree Balaji Site - minimal Vite + React + TypeScript setup

This project contains a Navbar component at src/components/Navbar.tsx and a small demo app to preview it.

Scripts:
- npm run dev    - start dev server
- npm run build  - build for production
- npm run preview - preview build
- npm run typecheck - run TypeScript typecheck
